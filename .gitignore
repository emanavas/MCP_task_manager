# SQLite database files (project-specific data — never commit)
task-manager/tasks.db
task-manager/tasks.db-shm
task-manager/tasks.db-wal

# Node
node_modules/

pnpm-workspace.yaml
tasks.db
tasks.db-shm
tasks.db-wal
